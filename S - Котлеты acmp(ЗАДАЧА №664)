# S - Котлеты acmp(ЗАДАЧА №664)
# На сковородке одновременно можно положить k котлет.
# Каждую котлету нужно с каждой стороны обжаривать m минут непрерывно.
# За какое наименьшее время удастся поджарить с обеих сторон n котлет?
#
# Входные данные
# В единственной строке записано три натуральных числа: k, m, n, не превышающих 30000.
#
# Выходные данные
# В единственную строку нужно вывести единственное число – минимальное время,
# за которое можно поджарить все котлеты.
#
# Примеры:
# №	        INPUT	    OUTPUT
# 1)	    1 1 1   	  2
# 2)  	    2 2 1	      4

import math


def main(k, m, n):
    """
    Сначала высчитываем сколько раз мы будем загружать сковороду.
    Для этого количество котлет делим на количество мест на сковороде.
    Если у нас останится хотя бы одна катлета нам прийдется использовать сковородку еще раз,
    для округления вверх используем метод 'ceil' из библиотеки 'math'. 
    Затем результат умножаем на два так как катлеты прожариваем с двух сторон и на время прожарки. 
    :param k: - количество мест.
    :param m: - время на обжарку.
    :param n:  - количество катлет.
    :return: result - время за которое можно поджарить все котлеты.
    """
    result = math.ceil(n / k) * 2 * m
    return result


def foo():
    """
    Функиця используется для ручного ввода, принимает значения затем передает их в функцию для вычислений.
    :param k: - количество мест.
    :param m: - время на обжарку.
    :param n:  - количество катлет.
    """
    k, m, n = map(int, input().split())
    print(main(k, m, n))


if __name__ == '__main__':
    foo()
    # Проганяем тесты.
    assert main(1, 1, 1) == 2
    assert main(2, 2, 1) == 4
    print('Тесты прошли успешно')
