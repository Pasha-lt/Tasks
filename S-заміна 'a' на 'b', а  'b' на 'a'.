# Напишіть програму, яка зчитує значення двох змінних a і b, потім змінює їх значення місцями
# (тобто в змінній a повинно бути записано те, що раніше зберігалося в b, а в змінній b записано те,
# що раніше зберігалося в a). Потім виведіть значення змінних.
# Вхідні дані
# Вводяться два цілих числа (гарантується, що числа знаходяться в діапазоні від -1000 до +1000).
# Вихідні дані
# Виведіть відповідь на задачу.
# Примітка
# Рішенням задачі не є такий код:
# ввід (a)
# ввід (b)
# вивід (b, a)
# Приклади
# вхідні дані
# 8
# 5
# вихідні дані
# 5
# 8



# 5 8
# ------ solution ------

a, b = map(int, input('input \'А\' and \'B\'').split())
a, b = b, a
print(a, b)
