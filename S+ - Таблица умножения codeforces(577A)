# S+ - Таблица умножения codeforces(577A)
# Рассмотрим таблицу из n строк и n столбцов. Известно, что в клетке,
# образованной пересечением i-й строки и j-го столбца, записано число i × j. Строки и столбцы нумеруются с единицы.
#
# Дано целое положительное число x. Требуется посчитать количество клеток таблицы, в которых находится число x.
#
# Входные данные
# В единственной строке находятся числа n и x (1 ≤ n ≤ 105, 1 ≤ x ≤ 109) — размер таблицы и число,
# которое мы ищем в таблице.
#
# Выходные данные
# Выведите единственное число: количество раз, которое число x встречается в таблице.
#
# Примеры
# входные данныеСкопировать
# 10 5
# выходные данныеСкопировать
# 2
# входные данныеСкопировать
# 6 12
# выходные данныеСкопировать
# 4
# входные данныеСкопировать
# 5 13
# выходные данныеСкопировать
# 0


def main():
    """Функция которая принимает значение от пользователя.
    Затем передает их в функции для построения матрицы и поиска повторений.
    n - размер матрицы (n x n).
    x - число которое мы ищем.
    """
    n, x = map(int, input().split())

    bild_matrix(n, x)


def bild_matrix(n, x):
    """
    Функция создает матрицу.
    Передает матрицу и число количество вхождений которого нужно посчитать.
    :param n: - размер матрицы (n x n).
    :param x:  - число которое мы ищем.
    """
    a = []
    for i in range(1, n+1):
        b = []
        for j in range(1, n+1):
            b.append(i*j)
        a.append(b)

    count(a, x)


def count(a, x):
    """
    функция считает слоко раз число 'x' встречается в нашей матрице и выводит результат.
    :param a:  Матрица
    :param x:  - число которое мы ищем.
    """
    count = 0
    for i in a:
        for j in i:
            if j == x:
                count += 1

    print(count)


if __name__ == '__main__':
    main()
