# моя почта 9139136@gmail.com
# Дана строка, в которой буква h встречается минимум два раза. Удалите из этой строки первое и последнее вхождение буквы h,
# а также все символы, находящиеся между ними.
# Входные данные
# Вводится строка.
# Выходные данные
# Выведите ответ на задачу.
# Примеры
# входные данные
# In the hole in the ground there lived a hobbit
# выходные данные
# In tobbit
# ------ solution ------

def countf(x):
    z = x.find('h')
    c = x.rfind('h')
    newh = x[:z] + x[c+1:] # Создаем новую строку в которой входит две части строки выше до первой и последней 'h'
    print(newh)
    main()


def main():
    x = input('Enter string')
    countf(x)


if __name__ == '__main__':
    main()
