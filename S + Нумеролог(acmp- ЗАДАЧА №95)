# S + Нумеролог(acmp- ЗАДАЧА №95)
# 
# Чтобы предсказать судьбу человека, нумеролог берет время жизни человека в секундах,
# затем складывает все цифры этого числа. Если полученное число состоит более чем из одной цифры,
# операция повторяется, пока в числе не останется одна цифра. Затем по полученной цифре и числу операций,
# необходимых для преобразования числа в цифру нумеролог предсказывает судьбу человека. Нумеролог плохо умеет считать,
# а числа, с которыми он работает, могут быть очень большими. Напишите программу, которая бы делала все расчеты за него.
# 
# Входные данные
# INPUT содержит число N – время жизни человека в секундах (1 ≤ N ≤ 101000).
# 
# Выходные данные
# OUTPUT выведите два числа через пробел: полученную цифру из числа N и число преобразований.
# 
# Примеры
# №	        INPUT	    OUTPUT
# 1	          1	       1 0
# 2	          10	     1 1
# 3	          99	     9 2


def main(n, count=0):
    """
    n - число в виде строки, время жизни человека.
    count - Количество преобразований.
    """
    if len(n) == 1:  # Определяем базовый случай рекурсии он будет когда длина числа равна одному символу.
        print(n, count)
    else:
        s = 0
        for i in n:
            s += int(i)
        main(str(s), count+1)


if __name__ == '__main__':
    s = input()
    main(s)
