# M - Художник acmp - №27
#
# Известный художник решил написать новый шедевр.
# После многих дней усердной работы он захотел исследовать свое творение.
# Художник вспомнил, что картина писалась следующим образом: сначала был взят белый холст,
# имеющий форму прямоугольника шириной w и высотой h.
# Затем художник нарисовал на этом холсте n прямоугольников со сторонами, параллельными сторонам холста и вершинами,
# расположенными в целочисленных координатах. Помогите художнику определить площадь незакрашенной части холста.
#
# Входные данные
# Первая строка входного файла INPUT.TXT содержит два натуральных числа w и h (1 ≤ w, h ≤ 100).
# Во второй строке записано целое число n (0 ≤ n ≤ 5000) – количество прямоугольников.
# Следующие n строк содержат информацию о всех прямоугольниках.
# Каждая строка описывает один прямоугольник в виде четырех чисел x1, y1, x2, y2 ,
# где (x1, y1) и (x2, y2) – координаты левого верхнего и правого нижнего угла прямоугольника соответственно.
#
# Выходные данные
# Выведите в выходной файл OUTPUT.TXT одно целое число – площадь незакрашенной части холста.
#
# Примеры
# №   	    INPUT.TXT	        OUTPUT.TXT
# 1)	       5 5
#            2
#            1 1 3 3
#            2 2 4 4	            18
#
# 2)         6 7
#            3
#            0 0 5 5
#            1 1 4 4
#            2 2 3 3	            17


def main():
    """
    Для того чтобы решить задачу мы построим матрицу размером (WxH) она у нас будет состоять из вложеных списков.
    Таким образом в цикле будем проходить координаты 'x' и 'y'. Первоначально наша матрица будет состоять из 1.
    Единица у нас будет аналогом пустой ячейки, ноль будет закрашеной ячейкой.
    Когда мы будем получать данные о зарисованных прямоугольниках ихние координаты мы будем превращать в ноль.
    Затем подсчитав сумму пустых ячеек мы получим результат который от нас требуется.
    Функция которая принимает данные для того чтобы определить размер матрицы.
    И передает данные в функцию которая будет создавать матрицу передаем(w, h, n)
    # w - Ширина холста.
    # h - Высота холста.
    # n - Нарисованые художником прямоугольники.
    """
    w, h = map(int, input('w, h').split())
    n = int(input('number of rectangles'))

    make_matrix(w, h, n)


def make_matrix(w, h, n):
    """
    Функция которая создает матрицу и наполяет ее единицами. Затем передает ее в функцию для 'зарисовки'.
    """
    a = []
    for i in range(w):
        b = []
        for j in range(h):
            b.append(1)
        a.append(b)

    sketching_matrix(n, a)


def sketching_matrix(n, a):
    """Функция запрашивает координаты зарисованых прямоугольников и проставляет нули по этим кординатам.
    Затем передает функцию для подсчета свободных клеток."""
    while n > 0:
        x_start, y_start, x_finish,  y_finish = map(int, input('координаты').split())
        n -= 1
        for x in range(x_start, x_finish):
            for y in range(y_start, y_finish):
                a[x][y] = 0

    result(a)


def result(a):
    """
    Функция используется для того чтобы подсумировать свободные клетки и вывести то что от нас требывалось в условии.
    """
    count = 0
    for i in a:
        count += sum(i)
    print(count)


if __name__ == '__main__':
    main()
