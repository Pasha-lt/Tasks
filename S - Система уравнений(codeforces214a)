# S - Система уравнений(codeforces214a)

# Фурик очень любит уроки математики, поэтому, в отличие от Рубика, он их не посещает.
# Но теперь Фурик хочет получить хорошую оценку по математике. Для этого Лариса Ивановна,
# учительница математики, дала ему новое задание. Фурик сразу же решил эту задачу, а вы сможете?

# Задана система уравнений:
# a**2 + b = n
# a + b**2 = m
# Нужно посчитать количество пар целых чисел (a, b) (0 ≤ a, b), которые удовлетворяют системе.
#
# Входные данные
# В единственной строке заданы два целых числа n, m (1 ≤ n, m ≤ 1000) — параметры системы.
# Числа в строке разделены пробелом.
#
# Выходные данные
# В единственную строку выведите ответ на задачу.
#
# Примеры
# входные данныеСкопировать
# 9 3
# выходные данныеСкопировать
# 1
# входные данныеСкопировать
# 14 28
# выходные данныеСкопировать
# 1
# входные данныеСкопировать
# 4 20
# выходные данныеСкопировать
# 0
# Примечание
# В первом примере подходит пара чисел (3, 0). Во втором примере подходит пара чисел (3, 5).
# В третьем примере не существует подходящей пары.



def foo(n, m):
    """Функция считает количество пар чисел которые при подстановке в системе дают правильный результат.
    count - результат, его и возвращает функция"""

    count = 0
    a = 0

    while a**2 <= n:
        b = n - a**2
        if b>=0 and a+b**2 == m:
            count+=1
        a+=1

    return count


def main():
    """Функция принимает значения от пользователя и передает их в ф-цию foo()"""
    
    n, m = map(int, input().split())
    return foo(n, m)


if __name__ == '__main__':
    main()
    assert foo(9, 3) == 1
    assert foo(14, 28) == 1
    assert foo(4, 20) == 0
    print('Тесты отработали, все верно!')
