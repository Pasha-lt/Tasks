# моя почта 9139136@gmail.com
# Слон
# Напомним, что в шахматах используется клеточная доска размером 8х8, где располагаются шахматные фигуры,
# которые могут перемещаться по определенным правилам. В частности, слон может перемещаться на любое расстояние по диагонали.
# Требуется определить: может ли слон выполнить ход из клетки с координатами (X1,Y1) в клетку с координатами (X2,Y2)
# на стандартной шахматной доске?
# Входные данные
# Первая строка входного файла INPUT.TXT содержит два целых числа X1 и Y1 – начальную координату слона.
# Во второй строке записана конечная координата – целые числа X2 и Y2 (1 ≤ X1,Y1,X2,Y2 ≤ 8). Гарантируется,
# что начальная и конечная координаты не совпадают.
# Выходные данные
# В выходной файл OUTPUT.TXT выведите «YES», если ход является допустимым, и «NO» - в противном случае.
# Примеры:
#          INPUT.TXT                         OUTPUT.TXT
# 1)         5 4                                YES
#            7 2

# 2)         5 4                                NO
#            4 6
# ------ solution ------


def main():
    with open('INPUT.txt', 'r') as f:
        s = f.read()
        a, b, a1, b1  = map(int, s.split())
        # We write the conditions under which the piece should be on the board.
        if 8 >= a >= 1 and 8 >= b >= 1 and 8 >= a1 >= 1 and 8 >= b1 >= 1:
            if abs(a - a1) == abs(b - b1):
                writefile = 'YES'
            else:
                writefile = 'NO'
        foo(writefile)


def foo(writefile):
    with open('OUTPUT.txt', 'w') as f:
        f.write(writefile)


if __name__ == '__main__':
     main()
