# S - Сумма матриц (acmp №1237)
# Заданы две целочисленные матрицы, каждая из которых состоит из N строк и M столбцов. Требуется вычислить их сумму.
#
# Входные данные
# Первая строка входного файла содержит два натуральных числа N и M – количество строк и столбцов каждой матрицы.
# Далее следует описание двух матриц. Каждая матрица состоит из N строк по M целых чисел.
# Матрицы отделены друг от друга пустой строкой. Все числа во входных данных не превышают 100 по абсолютной величине.
#
# Выходные данные
# Выведите матрицу, полученную в результате суммы заданных матриц.
#
# Пример
# №	      INPUT	             OUTPUT
# 	       3 4
# 1)    4 -2 6 1            6 -5 8 8
#       -1 7 -4 3           4 8 -5 3
#       5 3 9 4             3 6 5 6
#
# 2)    -3 2 7
#       5 1 -1 0
#       -2 3 -4 2


def main():
    """Функция принимает входные данные для размера в ширину и высоту матриц(n,m) и значения самих матриц.
    Также в этой функции мы создаем матрицу в которую затем запишем результат. Функция передает матрицы (a, b, c) и
    ширину и высоту матриц(n,m)
    :return:
    """
    n, m = map(int, input().split())
    a = []
    for i in range(n):
        a.append(list(map(int, input().split())))

    input()  # Ставим ввод чтобы принимать значения как в задаче.

    b = []
    for i in range(n):
        b.append(list(map(int, input().split())))
    c = [[0] * m for i in range(n)]  # Создаем мартицу 'c' с нулями в которую будем записывать сумму матриц 'a' и 'b'.
    counter(a, b, c, n, m)


def counter(a, b, c, n, m):
    """
    Функция принимает матрицы (a, b, c) и ширину и высоту матриц(n,m) высчитывает сумму матриц ('a' и 'b')
    и записывает результат в матрицу 'c' затем выводит ее.
    """
    for i in range(n):
        for j in range(m):
            c[i][j] = a[i][j] + b[i][j]
    print(c)


if __name__ == '__main__':
    main()
