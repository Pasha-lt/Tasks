# S - Зимний вечер в бурсе. 
# Возьмем число. Умножим его на его же первую цифру. Результат умножим на первую цифру результатаю И так далее.
# Например:
# 8 -> 8*8 = 64
# 64 -> 6*64 = 384
# 384 -> 3*384 = 1152
# 1152 -> 1*1152 = 1152

# Очевидно, когда первая цифра очередного числа в такой последовательности становится равной 1,
# числа перестают изменяться, Но это проиходит не при всех начальных числах.

# Напишем прогамму, которая будет хотя бы приблизительно определять судьбу введенного числа 'n'

# Начиная с числа 'n', умножайте имеющиееся число на его первую цифру,
# пока у получившегося числа первая цифра не станет равной 1, либо пока оно не привысит миллиарда.
# В качестве ответа выведите результат.


def foo(n):
    """Функция которая принимает число(n), перемножает на первую цифру,
     если число меньше миллиарда и первая цифра не является единицей повторяет операцию."""
    while str(n)[0] != '1' and n <= 1000000001:
        n = n * int(str(n)[0])
    return n


def main():
    """Функция принимает число от пользователя(n), передает его в функцию foo и возвращает результат функции foo """
    n = int(input())
    return(foo(n))


if __name__ == '__main__':
    print(main())

    assert foo(8) == 1152
    print('Тесты прошли успешно')
