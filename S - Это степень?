# S - Это степень?.
# В архитектуре компьютераважную роль играют числа, являющиеся степенями двойки: 1, 2, 4, 8 и так делее.
# Напишите программу, которая проверяет, является ли введенное число натуральное число степенью двойки.
# Если да, то выводится сама эта степень двойки; если нет, выводится "НЕТ"

# Sample Input 1:
# 8
# Sample Output 1:
# 3

# Sample Input 2:
# 9
# Sample Output 2:
# НЕТ


def main(number):
    """Функция проверяет является ли число 'number' степенью двойки.
    Возврщает в зависимости от ответа либо степень либо ответ 'НЕТ'.
    exponent - показатель степени.
    number - входящее число.
    """
    exponent = 0
    while True:
        exponent += 1
        number = number / 2
        if number == 1.0:  # если у нас будет остаток один это будет базовый случай если число является степенью 2.
            return exponent
        elif number == 2.0:
            continue
        elif number % 2 != 0:
            return 'НЕТ'


if __name__ == '__main__':
    print("Example:")
    assert main(8) == 3  # Делаем тесты для прогона результатов.
    assert main(16) == 4
    assert main(32) == 5
    assert main(9) == 'НЕТ'
    assert main(10) == 'НЕТ'
    print("Все работает корректно!")
